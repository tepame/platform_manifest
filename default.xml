<?xml version="1.0" encoding="UTF-8"?>
<manifest>

	<!-- remote origin definition -->
	<remote name="tepame"
		fetch="https://github.com/tepame"
		revision="master" />

	<!-- default origin source -->
	<default revision="master"
		 remote="tepame"
		 sync-c="true"
		 sync-j="4" />

	<!-- tepame-os projects -->
	<project path="platform_manifest" name="platform_manifest" groups="pdk" />

</manifest>
